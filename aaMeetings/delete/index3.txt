;
            function makeContent(cont) {
            var contentHolder = '<div id="content">' +
                '<div id="siteNotice">' +
                '</div>';
                
            for (var i = 0; i < cont.meetingGroups.length; i++) {
            
            if (i == 0) {
            contentHolder = contentHolder + '<h2>' + cont.meetingGroups[i].groupInfo.address + '</h2>';
            } 
           // console.log(cont.meetingGroups[i])
                contentHolder = contentHolder + '<h1 id="firstHeading" class="firstHeading">';
                
                
                
                
                contentHolder = contentHolder + cont.meetingGroups[i].groupInfo.meetingGroup+ '</h1>' +'</br>' +'<br>';
                
               meetingDay = cont.meetingGroups[i].meetingDay;
                
            // loop through days
                for (var j = 0; j<meetingDay.length; j++){
                
               contentHolder = contentHolder +  cont.meetingGroups[i].meetingDay[j] + ' ' +'<br>';
                
                              console.log(cont.meetingGroups[i].startTimeHour)

                
                     }
           
                
             // loop through hours 
             

             meetingHours = cont.meetingGroups[i].startTimeHour;
             
             
                for (var k = 0; k<meetingHours.length; k++){
                
                var hour = cont.meetingGroups[i].startTimeHour[k];
                var minutes = minCheck (cont.meetingGroups[i].startTimeMinutes[k]);
                
                function minCheck (min) {
                    if (min == 0){
                    min = "00";
                    return min;
                    } else { return min }
                }
                
                function miltostandard(militaryhour, minutes){
                
                if (militaryhour>12){
                return militaryhour-12 + ":" + minutes + " PM";
                } else {
                return militaryhour + ":" + minutes + " AM";
                }
                }
                
                contentHolder = contentHolder +  miltostandard(cont.meetingGroups[i].startTimeHour[k], minutes) + ' ' +'<br>';
                }
                
                

        ///End loop through hours/days
        
        
        
        
        
                contentHolder = contentHolder + '</p>';
                contentHolder = contentHolder + '<div class="iw-bottom-gradient"></div>';
            }
            return contentHolder;
        }

        function setMarkers(map, locations) {
            // Add markers to the map
            var infowindow = new google.maps.InfoWindow({
                minWidth: 300
            });

            for (var i = 0; i < locations.length; i++) {
                var meeting = locations[i];
                var myLatLng = new google.maps.LatLng(meeting._id.latLong);

                var contentWindow = makeContent(meetings[i]);
    
                
                var marker = new google.maps.Marker({
                    position: myLatLng,
                    map: map,
                    title: "Click for meeting info",
                    //this is where I can type hello and see it in my marker
                    content: contentWindow,
                });

                google.maps.event.addListener(marker, 'click', function() {
                    infowindow.setContent(this.content);
                    infowindow.open(map, this);
                });
            }
        }
        google.maps.event.addDomListener(window, 'load', initialize);
    </script>
</head>

<body>
    <div id="map-canvas"></div>
</body>

</html>